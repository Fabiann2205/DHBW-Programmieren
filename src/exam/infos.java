package exam;

public class infos {
    //Wichtige Informationen: in klassen bei while schleifen: außerhalb der while schleife schon variable erstellen, aber noch nicht die referenz
    // also die rechte seite der Zuweisung: -> diese erst in jedem durchlauf neu erstellen:
    // Boolean run = true;
    //     Scanner input = new Scanner(System.in);
    //     String eingabe;
    //     Field[] getroffene; <----HIER
    //     String[] vorher; <--------HIER
    //     int player = 0;
    //     int points;
    //     while(run) {
    //         System.out.println(player);
    //         // input auslesen
    //         System.out.println("Player: " + players[player].getName() + ", " + players[player].getRemainingPoints() + " points remaining.");
    //         System.out.print("Enter visit: ");
    //         eingabe = input.nextLine();
    //         vorher  = new String[3]; <----HIER
    //         vorher = eingabe.split(" ");
    //         points = 0;
    //         getroffene = new Field[3]; <----HIER
    //         for(int i = 0; i<vorher.length; i++) {
    //             getroffene[i] = this.board.parseField(String.valueOf(vorher[i]));
    //             points += getroffene[i].getVa
}

// auf jeden Fall .equals() Methoden benutzen
// auf jeden Fall arraylist benutzen